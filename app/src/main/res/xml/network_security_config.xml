<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <debug-overrides>
        <trust-anchors>
            <!-- Trust user added CAs while debuggable only -->
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <!--<domain-config>
        <domain includeSubdomains="true">digitalassetlinks.googleapis.com</domain>
        <trust-anchors>
            <certificates src="@raw/chls"/>
        </trust-anchors>
    </domain-config>-->

</network-security-config>